#put here your db settings

spring:
  datasource:
    driver-class-name:
    url:
    username:
    password:

  jpa:
    database:
    database-platform:
    show-sql:

    hibernate:
      ddl-auto:

    properties:
      hibernate:
        format_sql:


